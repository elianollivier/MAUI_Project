<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    x:Class="Elian_App.Views.HomePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <!-- Ajout du logo en haut de la page -->
            <Image
                Source="logo.png"
                HeightRequest="80"
                Aspect="AspectFit"
                HorizontalOptions="Center"
                SemanticProperties.Description="Logo de l'application" />

            <Label
                Text="Bienvenue sur l’Accueil !"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Ceci est la page d’accueil de l’application."
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2" />

            <!-- Bouton compteur d’exemple (issu de l’ex-MainPage) -->
            <Button
                x:Name="CounterBtn"
                Text="Cliquez moi"
                SemanticProperties.Hint="Compteur de clics"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>